import React from "react";

class Filters extends React.Component {
  render() {
    return (
      <form action="">
        <label htmlFor="character">Personaje</label>
        <input id="character" type="text" placeholder="Rick" />
      </form>
    );
  }
}

export default Filters;
